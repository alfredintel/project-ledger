# {{PROJECT}} — Overview & Frame

> The **frame**: what we're building and why. The stable north star. Edited rarely.
> For where-we-are, see the scoreboard (`docs/{{PREFIX}}_BUILD_STATUS.md`).

## What it is

{{One-paragraph thesis. The product/system in two or three sentences — what it does
and the core architectural separation that defines it.}}

## Destination

{{The end-state architecture, in prose or a node list. Where it's all going. This is
allowed to describe the full vision — the scoreboard is what keeps the vision honest
against current reality, so the frame can dream.}}

## Editorial constraint (status-label discipline)

Every external claim about a capability must carry a status label
({{STATUS_VOCAB}}). The destination renders every node identically; the labels are
what keep vision and reality from blurring. An unlabeled version overstates the build.

## Where are we?

Mirrors the ledger discipline:

- **Contract** — [`{{PREFIX}}-CONTRACT.md`](./{{PREFIX}}-CONTRACT.md): rules of engagement for agents. Read first.
- **Scoreboard** — [`docs/{{PREFIX}}_BUILD_STATUS.md`](./docs/{{PREFIX}}_BUILD_STATUS.md): every capability, honest status, live operational state. **Start here.**
- **Work queue** — [`{{PREFIX}}_OPEN_ITEMS.md`](./{{PREFIX}}_OPEN_ITEMS.md): open items + resolved trail.
- **Variance log** — [`{{PREFIX}}_VARIANCE_LOG.md`](./{{PREFIX}}_VARIANCE_LOG.md): what diverged.
- **Bugs log** — [`docs/bugs/bugs_log.md`](./docs/bugs/bugs_log.md): defects found → fixed.
- **Runbook** — [`docs/deploy/deploy_runbook.md`](./docs/deploy/deploy_runbook.md): deploy, access, roll back.
- **Testing** — [`docs/testing/testing_procedure.md`](./docs/testing/testing_procedure.md): how to run the tests.
- **Research** — [`docs/research/`](./docs/research/): deep-dives + explorations.
- **Briefs** — [`docs/briefs/`](./docs/briefs/): per-session intent + outcome.
